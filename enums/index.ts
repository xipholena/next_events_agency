export enum TicketStatus {
  FREE = 'free',
  RESERVED = 'reserved',
  SOLD = 'sold',
  USED = 'used',
  REFUND = 'refund'
}

export enum TagEnum {
  EVENT_TYPE = 'event_type',
  GENRE = 'genre',
  AGE = 'age',
  DURATION = 'duration',
  CITY = 'city',
}

export enum ReviewStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
}

export enum ROLES {
  User = 'user',
  Admin = 'admin',
  Moderator = 'moderator',
  Operator = 'operator'
}